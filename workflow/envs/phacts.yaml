name: phacts
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python>=3.7
  - pandas
  - numpy
  - biopython
  - matplotlib

# We're now using a simplified custom implementation
# No need for scikit-learn or other PHACTS dependencies