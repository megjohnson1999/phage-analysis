name: phacts
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python>=3.7
  - pandas
  - numpy
  - biopython
  - scikit-learn=0.24  # PHACTS requires this version 
  - matplotlib
  - libsvm  # Required for PHACTS

# Using the existing PHACTS installation at /home/luisalberto/Softwares/PHACTS/